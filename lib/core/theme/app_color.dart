import 'package:flutter/material.dart';

class AppColor {
  AppColor._();

  // 🎨 Primaire Orange (élégant et chaleureux)
  static const Color kPrimary = Color(0xFFFF7A00);     // Orange principal
  static const Color kPrimaryLight = Color(0xFFFFB066); // Orange clair
  static const Color kPrimaryDark = Color(0xFFE06A00);  // Orange profond

  // ⚫️ Noir et nuances (écriture et fonds)
  static const Color kBlack = Color(0xFF0D0D0F);        // Noir profond (textes forts)
  static const Color kGrayscaleDark100 = Color(0xFF1E1E1E); // Gris très foncé
  static const Color kGrayscale80 = Color(0xFF2C2C2C);  // Gris foncé
  static const Color kGrayscale60 = Color(0xFF4A4A4A);  // Gris moyen
  static const Color kGrayscale40 = Color(0xFF6B7280);  // Gris clair (sous-titres)
  static const Color kGrayscale20 = Color(0xFFD1D5DB);  // Gris très clair
  static const Color kLine = Color(0xFFE5E7EB);         // Lignes de séparation

  // 🟤 Accents subtils
  static const Color kAccentSoft = Color(0xFFFFF4E8);   // Fond d'accent orangé très doux

  // 🧻 Blanc et fonds
  static const Color kWhite = Color(0xFFFFFFFF);
  static const Color kBackground = Color(0xFFF9FAFB);
  static const Color kBackground2 = Color(0xFFF3F4F6);
  static const Color kOnBoardingColor = Color(0XFFFEFEFE);
}